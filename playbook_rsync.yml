  # Копирование артефакта из агента в директорию /tmp
  - name: "Get imdex.html from repozitory"
    hosts: all
    become: yes
    tasks:
      copy:
      - name: Копируем
        src: ./index.html
        dest: /opt/dockersite/
        mode: 0775
      - name: "Debug"
        ansible.builtin.debug:
        msg: Получилось
